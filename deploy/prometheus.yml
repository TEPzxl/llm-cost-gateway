global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: llm-cost-gateway
    metrics_path: /metrics
    static_configs:
      - targets:
          - gateway:8080
